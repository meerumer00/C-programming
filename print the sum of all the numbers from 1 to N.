//WAP to print the sum of all the numbers from 1 to N.
#include<stdio.h>
int main(){
    int i,n,sum=0;
    printf("Enter the value of N");
    scanf("%d", &n);
    for(i=0; i<=n; i=i++){
        sum += i;
    }
    printf ("sum=%d", sum);
    return 0;
}
