#include <stdio.h>

int main() {
    int n, i;
    scanf("%d", &n);
    int arr[n];
    int *p = arr;
    for (i = 0; i < n; i++) scanf("%d", p + i);
    for (i = 0; i < n; i++) printf("%d ", *(p + i));
    return 0;
}
