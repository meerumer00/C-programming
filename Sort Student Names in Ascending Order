#include <string.h>

int main() {
    int n;
    scanf("%d", &n);  // number of students

    char names[n][100]; // assuming max length of a name = 99 chars

    // input student names
    for (int i = 0; i < n; i++) {
        scanf("%s", names[i]);
    }

    // sort names in ascending order
    char temp[100];
    for (int i = 0; i < n - 1; i++) {
        for (int j = i + 1; j < n; j++) {
            if (strcmp(names[i], names[j]) > 0) {
                strcpy(temp, names[i]);
                strcpy(names[i], names[j]);
                strcpy(names[j], temp);
            }
        }
    }

    // print sorted names
    for (int i = 0; i < n; i++) {
        printf("%s\n", names[i]);
    }

    return 0;
}
